#!/usr/bin/env bash
set -e
if [[ ! -d "/home/tvconss/Workspace/sea-hackathon-2026/web" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/tvconss/Workspace/sea-hackathon-2026/web")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/tvconss/Workspace/sea-hackathon-2026/web" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/tvconss/Workspace/sea-hackathon-2026/web/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/tvconss/Workspace/sea-hackathon-2026/web/.envrc" "/home/tvconss/Workspace/sea-hackathon-2026/web/.direnv"/*.rc
